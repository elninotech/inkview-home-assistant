# Test dependencies. pytest-homeassistant-custom-component pulls in a matching
# Home Assistant core, pytest, and pytest-asyncio, so the suite runs against a
# real hass (state machine, entity registry, etc.).
#
# Pinned so CI reproduces exactly what the suite was verified against. 0.13.205
# brings HA 2025.1.x (the last line supporting Python 3.12); bump this together
# with the CI Python version when raising the tested HA baseline.
pytest-homeassistant-custom-component==0.13.205
pytest-cov
